from dragonfly import (Grammar, 
                       AppContext, 
                       MappingRule, 
                       Dictation, 
                       InegerRef)

vscode_context = AppContext(executable="putty")
